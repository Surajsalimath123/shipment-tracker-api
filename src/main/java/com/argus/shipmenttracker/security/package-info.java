/**
 * JWT authentication, Spring Security configuration, and the
 * {@code TenantContext} that propagates the authenticated company_id
 * through the request lifecycle.
 */
package com.argus.shipmenttracker.security;
