/**
 * REST controllers exposing the v1 HTTP API. Controllers are thin: they
 * validate input, delegate to services, and shape responses. No business
 * logic lives here.
 */
package com.argus.shipmenttracker.controller;
